import 'package:flutter/material.dart';

class ScribblyText extends StatelessWidget {
  const ScribblyText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("");
  }
}
